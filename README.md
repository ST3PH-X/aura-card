# aura-card
Standalone zero-dependency WebGL/GLSL engine rendering unique responsive Julia Set fractals in 60 FPS natively on the GPU. Parses plain text inputs into mathematical space coordinates (fractal DNA), supports mobile-responsive layout tracking, and native single-click standalone PNG and animated infinite-loop SVG exports.
